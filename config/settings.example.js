/*
 * Portable settings template.
 *
 * Use control.html -> Performance -> Portable Settings to export the real
 * config/settings.js file. That local file is intentionally excluded from
 * Git because it may contain market-data API credentials.
 */
// window.PTE_SETTINGS = { ... };

